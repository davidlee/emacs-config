<!-- Project governance pointers — seeded once by `doctrine install`, then
     YOURS to edit. Projected into the session boot snapshot by `doctrine boot`.

     REMIT (keep it narrow). This is a *pointer/digest* layer, not a fourth
     source of truth. Carry only short, stable orientation an agent needs every
     session and that no other surface already projects: cross-references and
     one-line "where things live" notes. Do NOT restate —
       - CLAUDE.md / AGENTS.md  → live commands, layout, CLI, conventions
       - doc/*                  → evergreen authoritative specs (link, never copy)
       - ADRs                   → decisions + rationale (the boot snapshot already
                                  projects accepted ADRs in its own section)
     If a line belongs in one of those, put it there and at most point at it here.
     When in doubt, it does NOT go in this file. -->

# Governance (project)

## Where things live

<!-- e.g. -->
<!-- - Architecture spec: doc/architecture.md -->
<!-- - Build / test entrypoints: justfile (`just check`) -->
<!-- - Domain glossary: .doctrine/glossary.md -->

## Project-local rules of the road

<!-- Short, stable cross-references an agent needs every session. One line each. -->
