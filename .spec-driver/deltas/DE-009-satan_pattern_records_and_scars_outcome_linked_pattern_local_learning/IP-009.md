---
id: IP-009
slug: satan_pattern_records_and_scars_outcome_linked_pattern_local_learning
name: Implementation Plan - SATAN pattern records and scars - outcome-linked pattern-local learning
created: "2026-06-03"
updated: "2026-06-03"
status: draft  # one of: completed | deferred | draft | in-progress | pending
kind: plan  # one of: audit | delta | design_revision | issue | memory | phase | plan | policy | problem | prod | requirement | risk | spec | standard | task | verification
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-009
delta: DE-009
revision_links:
  aligns_with: []
specs:
  primary: []
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-009-P01
  - id: IP-009-P02
  - id: IP-009-P03
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-009
entries:
  - artefact: VT-intervention-percept-snapshot
    kind: VT
    requirement: DE-009
    status: planned
    notes: P01 — intervention.created stamps percept_handles_json from ctx; nil → []
  - artefact: VT-pattern-containment
    kind: VT
    requirement: DE-009
    status: planned
    notes: P02 — JSONB @> subset (empty/superset/disjoint) via DB fixture
  - artefact: VT-pattern-sync
    kind: VT
    requirement: DE-009
    status: planned
    notes: P02 — patterns.eld parse, idempotent sync, soft-retire, reject ungrammatical handle
  - artefact: VT-pattern-rebuild
    kind: VT
    requirement: DE-009
    status: planned
    notes: P02 — projection from seeded data; disabled still attributed; mature/non-unknown; head-only
  - artefact: VT-rebuild-guard
    kind: VT
    requirement: DE-009
    status: planned
    notes: P03 — rebuild + load/migration failure swallowed; classification + global state intact
  - artefact: VT-global-attr-regression
    kind: VT
    requirement: DE-009
    status: planned
    notes: P03 — global-attribute path + satan_attribute_events unchanged
  - artefact: VA-pattern-attribution
    kind: VA
    requirement: DE-009
    status: planned
    notes: P03 — seeded mature outcome + snapshot → real rebuild → inspect stats/scars
```

## 1. Summary

- **Delta**: DE-009 — SATAN pattern records and scars (outcome-linked pattern-local learning)
- **Design**: [DR-009](./DR-009.md) (accepted; internal + codex adversarial passes integrated)
- **Specs Impacted**: none (delta-first repo, no tech specs on disk)
- **Desired Outcome**: each mature intervention outcome accrues counters/scars against
  the curated pattern(s) whose cue-shape was present when it fired — derived as a
  deterministic, rebuildable SQL projection, with no change to the global-attribute path.

## 2. Context & Constraints

- **Current Behaviour**: observer classifies matured interventions → global attribute
  deltas only; nothing accrues per cue-shape.
- **Target Behaviour**: DR-009 §3 — percept-handle snapshot on interventions +
  containment-based pattern projection rebuilt at tick end.
- **Dependencies**: none external; builds on 0006 interventions projection + observer.
- **Constraints**: structural non-regression of global path (R3); audited/immutable
  attribution inputs (R4); forward-only migration; `just check` green; elisp paren-check
  per `.el`; new `.el` `git add`ed before `home-manager switch`.

## 3. Gate Check

- [x] Backlog/risks linked (R1–R4 dispositioned in DE-009; IMPR-010 related)
- [x] Delta specifies required changes (no specs in this repo)
- [x] Test strategy identified (ERT VTs + VA; DB fixtures; regression guard)
- [x] Workspace/config changes assessed (new `.el` + `.eld` → git add + home-manager switch)

> Tip: Plan phases up front, then create the phase sheet for the current phase only. Update later phases when you are ready to execute them.

## 4. Phase Overview

| Phase | Objective | Entrance Criteria | Exit Criteria / Done When | Phase Sheet |
| --- | --- | --- | --- | --- |
| P01 — Schema + percept snapshot | `0007` migration (interventions ALTER + GIN, `satan_patterns`, `satan_pattern_outcomes`, stats view); thread `:percept-handles` into tool-ctx; stamp `percept_handles_json` at `intervention.created` (payload + validator + insert) | DR-009 accepted | Migration applies; new interventions carry snapshot; `VT-intervention-percept-snapshot` green; existing intervention/outcome + global tests still green | `phases/phase-01.md` |
| P02 — Definitions + rebuild | `patterns.eld`; `dl-satan-pattern.el` (grammar-validated sync; `satan-pattern-rebuild` SQL, single tx + advisory lock, mature/non-unknown, head-only); read accessors | P01 complete | `VT-pattern-containment`, `VT-pattern-sync`, `VT-pattern-rebuild` green; rebuild populates projection + stats from seeded data | `phases/phase-02.md` |
| P03 — Observer wiring + guards + verification | Guarded/isolated `satan-pattern-rebuild` at observer tick end; regression + guard tests; VA; docs (CHANGELOG, epistemics-roadmap tracking) | P02 complete | `VT-rebuild-guard`, `VT-global-attr-regression`, `VA-pattern-attribution`; `just check` green; docs updated | `phases/phase-03.md` |

_Single migration `0007` lands all schema in P01 so P02 is elisp-only. Phase sheets authored one at a time._

## 5. Phase Detail Snapshot

- **Research Notes**: `DE-009/notes.md` (Phase 0 output)
- **Design Revision**: `DE-009/DR-XXX.md`
- **Active Phase Sheet**: <link once created>
- **Parallelisable Work**: Flag tasks with `[P]` inside phase sheets
- **Plan Updates**: Update this plan when phase outcomes change (new risks, scope adjustments)

## 6. Testing & Verification Plan

- **New Cases**: containment match (empty/superset/disjoint); snapshot stamping (incl.
  nil→`[]`); sync idempotency + grammar rejection; rebuild attribution (disabled still
  attributed, mature/non-unknown only, revised-away drops); guard isolation; global-path
  regression; VA on seeded mature outcome.
- **Tooling/Fixtures**: seeded DB rows (intervention + percept snapshot + outcome);
  reuse 0006/attribute test DB harness (jailed Postgres, DE-004/DE-006).
- **Rollback**: remove observer rebuild call; schema is inert vs global path.
- **Verification Coverage**: see `supekku:verification.coverage@v1` above (7 entries → phases).

## 7. Risks & Mitigations

| Risk | Mitigation | Owner |
| --- | --- | --- |
| R3 global-path regression | Rebuild last + guarded + module require isolated; `VT-rebuild-guard` + `VT-global-attr-regression` | Dev |
| R4 attribution determinism | Audited immutable snapshot column; pure SQL containment | Dev |
| Full-rebuild cost at volume | Set-based SQL + GIN; incremental rebuild is the escape hatch | Dev |
| New `.el`/`.eld` invisible to Nix | `git add` before `home-manager switch`; paren-check per file | Dev |

## 8. Open Questions & Decisions

- [ ] Rebuild cost at higher outcome volume (revisit incremental if needed) — non-blocking.
- [ ] Live read/match path (brings `last_seen_at`, `cooldown_until`) — deferred follow-up.

## 9. Progress Tracking

- [ ] Phase 0 complete
- [ ] Phase 1 complete
- [ ] Phase 2 complete
- [ ] Verification gates passed

## 10. Notes / Links

- Audit reference: AUD-XXX (pending)
- Supporting docs: <links>

```

```
