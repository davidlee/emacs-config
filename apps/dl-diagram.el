;;; dl-diagram.el --- Diagramming -*- lexical-binding: t; -*-

;; uniline removed: emacs-overlay's use-package scanner matches commented
;; forms too, so a `;; (use-package uniline)` line still installed it. The
;; package's native-comp is broken (void-function uniline--color-hint in
;; uniline-hydra.el). Re-add only when upstream fixes it.

(provide 'dl-diagram)
;;; dl-diagram.el ends here
